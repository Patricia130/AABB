# codemagic.yaml
#
# Configuração completa para build de um projeto Android Nativo no Codemagic.
# Este workflow gera os artefatos APK e AAB em modo release.

workflows:
  android-native-release:
    name: Tô Chegando Cliente - Build Release
    instance_type: mac_mini_m1
    max_build_duration: 60
    environment:
      android_sdk_version: "34" # Use a versão do SDK do Android que seu projeto precisa
      java_version: "17"       # Define a versão do Java para 17

    scripts:
      - name: ⚙️ Configurar permissões do Gradle Wrapper
        script: | 
          echo "✅ Concedendo permissão de execução para o gradlew..."
          chmod +x ./gradlew

      - name: 🧹 Limpar o projeto (Clean)
        script: | 
          echo "⚙️ Executando a tarefa 'clean' do Gradle..."
          ./gradlew clean

      - name: 📦 Gerar APK de Release
        script: | 
          echo "⚙️ Compilando e gerando o APK de release..."
          ./gradlew assembleRelease

      - name: 📦 Gerar AAB de Release (Bundle)
        script: | 
          echo "⚙️ Compilando e gerando o AAB de release..."
          ./gradlew bundleRelease

    artifacts:
      - name: "APK de Release"
        path: app/build/outputs/apk/release/*.apk
      - name: "AAB de Release"
        path: app/build/outputs/bundle/release/*.aab

    publishing:
      email:
        recipients:
          - seuemail@dominio.com
        notify:
          success: true
          failure: true